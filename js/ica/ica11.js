function random(number) {
    return Math.floor(Math.random() * number);
}

function draw() {
    // concole.log("TEST");
    ctx.clearRect(0,0, canvas.width, canvas.height);
    for(let i = 0; i< 100; i++){

    }
    
}

BigInt.addEventListener("click", draw);